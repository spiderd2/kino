{% extends "SosnowiecKinoBundle::base.html.twig"%}

{% block pageTitle %}
    Strona główna | {{parent()}}
{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <!-- HTML5 shim and Respond.js for IE8 support of HTML5 elements and media queries -->
    <!--[if lt IE 9]>
      <script src="https://oss.maxcdn.com/html5shiv/3.7.2/html5shiv.min.js"></script>
      <script src="https://oss.maxcdn.com/respond/1.4.2/respond.min.js"></script>
    <![endif]-->
    <link href="{{asset('bundles/sosnowieckino/js/parsley/parsley.css')}}" rel="stylesheet">
    <style>
        body {
            padding-bottom: 40px;
            background-color: #eee;
        }
        form {
            max-width: 430px;
            padding: 25px;
            margin: 0 auto;
            background-color: white;
            border-radius: 25px;
        }     
    </style>

{%endblock%}    


{% block pageContent %}
    <div class="row" style="padding-top:80px;">
        <div class="container col-md-4 col-md-offset-4">
            {% if error is defined%}
                <div>{{ error.messageKey|trans(error.messageData, 'security') }}</div>
            {% endif %}

            <form class="form-horizontal" method="POST" action="{{path('sosnowiec_kino_login')}}" data-validate="parsley">
                <h2 class="form-signin-heading">Logowanie</h2>
                <!-- <label for="inputEmail" class="sr-only">Email</label>-->
                <input type="email" id="inputEmail" class="form-control" name="_username" value="{% if last_username is defined%}{{ last_username }}{% endif %}" placeholder="Email" required autofocus data-trigger="change" data-reguired="true" data-type="email">
                <br>
                <!--<label for="inputPassword" class="sr-only">Hasło</label>-->
                <input type="password" id="inputPassword" class="form-control" name="_password" placeholder="Hasło" required data-trigger="change" data-reguired="true">
                <br>
                <button class="btn btn-lg btn-primary" type="submit">Zaloguj</button>
                <a href="{{path('sosnowiec_kino_register')}}">Zarejestruj</a>
            </form>

        </div> <!-- /container -->
        {%if msg is defined%}
            <div class="alert-danger fade in">
                <strong>{{msg}}</strong>
            </div>
        {%endif%}
    </div>
{%endblock%}

{% block javascripts %}
    {{ parent() }}
    <script src="{{asset('bundles/sosnowieckino/js/parsley/parsley.min.js')}}"></script>
    <script src="{{asset('bundles/sosnowieckino/js/parsley/parsley-standalone.min.js')}}"></script>
    <script src="{{asset('bundles/sosnowieckino/js/parsley/parsley.extend.min.js')}}"></script>

{%endblock%} 